specVersion: "v1"
serviceId: "medai"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "medai-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
